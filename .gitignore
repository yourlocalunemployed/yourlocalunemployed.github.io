
# Hugo build output — Cloudflare Pages rebuilds this from source on every push,
# so committing it is redundant (and makes noisy diffs / risks a stale copy).
public/
.hugo_build.lock

# local backup archives (stored off-repo, never commit)
blog-backups/
*.tar.gz
